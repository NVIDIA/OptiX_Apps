#ifndef PANE_FLAGS_H
#define PANE_FLAGS_H

struct PaneFlags {
    bool do_reference;
    bool do_ris;
    bool do_spatial_reuse;
    bool do_temporal_reuse;
    bool do_nrc;
};

#endif // PANE_FLAGS_H
